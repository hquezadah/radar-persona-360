{
  "name": "radar-persona-360",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de monitoreo y decisión para riesgo crediticio de personas.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "snapshot": "python3 -m pipeline.build_snapshot --input data/reference/segment_snapshot.csv --output data/monitoring_snapshot.json --as-of 2026-05-31",
    "check": "node --check app.js && node --test tests/dashboard.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "engines": {
    "node": ">=20"
  }
}
